<Window x:Class="BrowserDemo.Views.AiModelSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI 设置 — 模型选择"
        Height="520" Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E22"
        Foreground="#EEE"
        FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="模型选择" FontSize="16" FontWeight="SemiBold" Foreground="#EEE" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="＋" Width="32" Height="28" Click="Add_Click"
                    Background="#0078D4" BorderThickness="0" Foreground="White" FontSize="16"
                    Cursor="Hand" ToolTip="新增模型"/>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
                  HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal" Background="#202026" Foreground="#EEE"
                  RowBackground="#24242A" AlternatingRowBackground="#202026"
                  BorderBrush="#3A3A3E" HorizontalGridLinesBrush="#3A3A3E">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Black"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#3A3A3E"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="使用" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <RadioButton IsChecked="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Checked="ActiveRadio_Checked" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="模型名称" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4,3">
                                <TextBlock Text="{Binding Settings.DisplayName}" Foreground="#EEE" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Summary}" Foreground="#888" FontSize="11"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="默认" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <RadioButton IsChecked="{Binding IsDefault, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Checked="DefaultRadio_Checked" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="操作" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="编辑" Click="Edit_Click" Tag="{Binding}"
                                        Background="Transparent" BorderThickness="0" Foreground="#4FC3F7"
                                        Padding="4,2" Cursor="Hand"/>
                                <Button Content="删除" Click="Delete_Click" Tag="{Binding}"
                                        Background="Transparent" BorderThickness="0" Foreground="#FF8080"
                                        Padding="4,2" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" BorderBrush="#3A3A3E" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="StatusText" Foreground="#888" FontSize="11" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="取消" Width="80" Height="32" Click="Cancel_Click"
                        Background="#3A3A3E" BorderThickness="0" Foreground="#DDD" FontSize="12"
                        Margin="0,0,8,0" Cursor="Hand"/>
                <Button Grid.Column="2" Content="保存" Width="80" Height="32" Click="Save_Click"
                        Background="#0078D4" BorderThickness="0" Foreground="White" FontSize="12"
                        Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>
